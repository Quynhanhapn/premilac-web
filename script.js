const slides=[...document.querySelectorAll('.hero-slide')],dots=[...document.querySelectorAll('.dot')];let current=0;function show(i){current=i;slides.forEach((s,n)=>s.classList.toggle('active',n===i));dots.forEach((d,n)=>d.classList.toggle('active',n===i))}dots.forEach((d,i)=>d.onclick=()=>show(i));setInterval(()=>show((current+1)%slides.length),5000);const t=document.querySelector('.menu-toggle'),n=document.querySelector('.main-nav');t.onclick=()=>n.classList.toggle('open');n.querySelectorAll('a').forEach(a=>a.onclick=()=>n.classList.remove('open'));const topBtn=document.querySelector('.to-top');window.onscroll=()=>topBtn.classList.toggle('show',scrollY>500);topBtn.onclick=()=>scrollTo({top:0,behavior:'smooth'});
